services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: producttext
      POSTGRES_USER: producttext
      POSTGRES_PASSWORD: producttext
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: